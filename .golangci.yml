linters:
  enable:
    - gosec
    - govet
    - errcheck
    - staticcheck
    - ineffassign
    - unused

linters-settings:
  errcheck:
    exclude-functions:
      - (net/http.ResponseWriter).Write
      - (*encoding/json.Encoder).Encode
  gosec:
    excludes:
      - G115
